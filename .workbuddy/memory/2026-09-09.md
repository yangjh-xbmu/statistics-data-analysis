# 2026-09-09 工作日志

## agent-skills 同步核查（无改动）
- 用户请求「从 GitHub agent-skills 获取最新全局 skill」。
- 结论：GitHub 远端（a763ac4）不领先本地；本地 repos/agent-skills 反超 2 个提交（82012b6 workbuddy/codebuddy 分发目标、f7a931f learn-studio guide mode 吸收 concept-learning-guide）。manifest 中 workbuddy=true 且 managed 的 54 个技能在 ~/.workbuddy/skills 全部存在且逐文件一致（0 差异）。无需 pull/sync。用户选择不 push，保留本地领先状态。

## learn-studio guide 讲解：Agent 记忆系统
- 依 learn-studio 的 guide 模式（references/concept-guide.md）产出单页溯源 HTML：`agent-memory-guide.html`（放项目根目录）。
- 结构：PART A 通用 Agent 记忆系统（无状态问题→记忆分型→读写回路+上下文工程三策略→铁律）+ PART B WorkBuddy 三层记忆落地（L1 云端只读画像 / L2 ~/.workbuddy/MEMORY.md 用户级显式 / L3 .workbuddy/memory/ 项目级）+ 溯源(06) + 三级自测(6题)。
- WorkBuddy 部分一手证据取自本机文件（已实测存在）。
- 已通过 QC：JS 语法 OK、marker id 唯一、单强调色、quiz CSS 作用域收窄。

## learn-studio guide 模式：Agent 记忆系统溯源手册
- 产出 `agent-memory-system-guide.html`（项目根目录），讲解通用 agent 记忆系统 + WorkBuddy 三层记忆设计。
- 溯源一手来源：Generative Agents (arXiv:2304.03442)、MemGPT (arXiv:2310.08560)、Anthropic Memory Tool 官方文档、WorkBuddy 官方文档 + 腾讯云社区架构深读（交叉验证）。
- 结构：01 无状态 vs 有记忆 → 02 三大范式（记忆流/分层换页/文件即记忆）→ 03 四步循环（写入→蒸馏→检索→注入）→ 04 WorkBuddy 三层（云端画像/用户级 MEMORY.md/工作空间日志+30天蒸馏）→ 05 铁律 → 06 溯源 → 07 六题三级自测。
- 质检：node --check 通过，答案分布 1/2/3/0/3/2，SVG id 唯一，无 emoji。
- 备注：concept-learning-guide skill 已于 2026-09-03 并入 learn-studio 的 guide 模式（git f7a931f）。

## llm-wiki-guide 讲解 + SamurAIGPT/llm-wiki-agent 安装
- 用户说"使用guide skill 讲解llm wiki，并在当前目录安装 github中的llm wiki agent"。本机无独立 guide skill，但项目根已有系列 *-guide.html 模板（agent-guide / agent-skills-guide / agent-memory-guide / agent-memory-system-guide / llm-context-window-guide），约定俗成的"guide skill"指 learn-studio guide 模式。沿用系列 CSS（白底 + #C4633F 单强调色 + 衬线大标题 + U 型 6 节 + 三级 6 题自测）。
- 产出 `llm-wiki-guide.html`：01 它是什么（raw→LLM→wiki 编译图）/ 02 与 RAG 区别（上下两行对比图）/ 03 三层结构（不可变·知识·图谱）/ 04 工作流（4 个动词 + health/lint 分工）/ 05 五条铁律 / 06 溯源（Karpathy gist + SamurAIGPT 仓库 + markitdown）/ 07 三级自测 6 题。已用 agent-browser 全页截屏自验排版正常。
- 选 SamurAIGPT/llm-wiki-agent（3499 stars）而非 fcwu/llm-wiki-agent（1 star）：前者范式更完整、CLAUDE.md/AGENTS.md/GEMINI.md 多 agent 兼容、内置 health/lint/graph Python 工具，后者只是带 setup.sh 的样板。
- 装前按 skill 审计原则做了危险特征扫描（curl|sh / rm -rf / eval / base64 / 凭据外传等）：无命中。CLAUDE.md 与 README 仅为指令文档；tools/*.py 仅本地文件读写（grep 出的 reverse=True 均为排序键）。结论：P2 安全，可安装。
- `git clone --depth 1 https://github.com/SamurAIGPT/llm-wiki-agent.git` 已安装到当前目录（427KB，33 文件）。后续使用：cd llm-wiki-agent → claude → "ingest raw/foo.md"。

## llm-wiki 批量 ingest：6 份概念学习 HTML → 19 个 wiki 页
- 用户请求"将仓库中的概念学习 html 加入到 llm wiki 中"。源 = 项目根 6 份 *-guide.html（agent-guide / agent-memory-guide / agent-memory-system-guide / agent-skills-guide / llm-context-window-guide / llm-wiki-guide）。先 cp 到 llm-wiki-agent/raw/guides/（raw 不可变），再按 CLAUDE.md 工作流产出 wiki 三类页面。
- 产出：6 source 页（每页 markdown 转写，含 Key Claims / Key Quotes / Connections 三段） + 9 concept 页（Agent / ReAct / AgentMemory / ContextEngineering / ContextWindow / LostInTheMiddle / AgentSkills / LLMWiki / RAG，含定义/原理/反模式/相关概念） + 3 entity 页（Anthropic / AndrejKarpathy / WorkBuddy） + index.md（已同步） + overview.md（一条主线"Agent 把通用 LLM 变成会干活、能记得、可复用的专家"+ 两条支线"记忆系统为何三层 / 上下文满了怎么办"+ 跨源观察） + log.md（7 条 ingest 记录）。
- 验证（hit LLM 频率上限后重做）：health.py 全绿（0 空文件、0 索引不一致、0 日志缺失）；lint.py 结构检查 broken wikilinks=0、missing entities(3+)=0、6 个 orphan 全是 source 页（设计上正确——source 是被引用方而非被链接方）。
- 期间遇到的伪断链问题：`[[wikilinks]]` 字面量（在 source 页讲解双链语法时出现）和 `[[引用]]` 字面量（在概念页描述"内联引用"时出现），都被 lint 当作双链识别。修复方法：把双链括号外的文字换成自然描述（"Obsidian 风格的 wikilinks 双链"/"内联引用"）。教训：在 wiki 里讨论双链语法本身时，避开 `[[X]]` 字面量。
- 总计 21 个 md 写入 wiki/（含 metadata）。频率限制后用结构工具（lint.py 非 LLM 部分 + 自写脚本）验证完成，未跑 lint 的 LLM 评估。

## llm-wiki 自动化运维（recurring 21:09）
- 用户要求"将知识库的维护工作自动化，每天 21:09 开始整理，然后基于对宏哥的了解，发掘当下最有价值的概念"。
- 创建 automation `a6e7b4ca-6510-4375-9fab-bed51fb723f8`（name: "llm-wiki 每日维护 + 价值概念发掘"），scheduleType=recurring，rrule=`FREQ=DAILY;BYHOUR=21;BYMINUTE=9`，validFrom=2026-09-09。
- Prompt 内嵌 6 类关注点（教学/AI 工具链/大数据课程/科研效率/可视化教学法/工作流）+ 三维度打分（关注度 50% / 覆盖薄弱 30% / 时效热度 20%）+ Top 5 输出格式 + 写日志约定。
- 自动化跑完后给宏哥即时回报 Top 5（避免等到明天才看到效果）：①费曼学习法/概念可视化教学 ②大数据与AI教学（新开课程）③Prompt Caching ④Subagent 子代理架构 ⑤知识图谱认知科学基础（Zettelkasten/Andy Matuschak）。理由：教学是核心身份（费曼 + 大数据课程）权重最高；Prompt Caching 和 Subagent 与 ContextEngineering 紧密相关且 2026 持续演进；Zettelkasten 是 LLM Wiki 双链的认知科学根基。
- 待办：下次对话让宏哥从这 5 个候选里挑 1-2 个优先 ingest。
