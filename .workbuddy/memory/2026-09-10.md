# 2026-09-10 工作日志

## 排查「WorkBuddy 本地模型不能使用」
- 起因：上一会话（9b5faa19）用户问「如何在 workbuddy 中使用本地模型」，答案基于官方文档给了 Ollama 自定义模型接入流程；本次用户要求检查为什么本地模型不能使用。
- 结论：本机上两条本地模型通道都是空的，环境本身没问题。
  - 通道 A（设置→模型→「本地模型」，内置 llama.cpp 运行时）：`C:\Users\yangjh\.workbuddy\local-models` 目录不存在；daemon/main 日志中 0 条 `[LocalModel]` / `llama-server` 记录 → 运行时从未下载。运行时路径由 `get binDir()` 决定：`{WORKBUDDY_DATA_DIR|~/.workbuddy}/local-models/bin`，二进制为 `<bin>/<version>/llama-server.exe`。运行时 ready=false 时 `useLocalModels` 会把已启用的本地模型自动关闭（关键坑）。
  - 通道 B（设置→模型→添加自定义模型→「Ollama 本地」）：`C:\Users\yangjh\.workbuddy\models.json` 不存在（应用 `[CustomModelsProvider] watching ...models.json for changes` 一直等不到文件）；`~/.codebuddy/models.json` 与 `<workspace>/.codebuddy/models.json` 同样不存在；`[Merge] Step 2-3 (+enterprise +custom): enterpriseModels=0, customModels=0, totalModels=46` → 自定义模型数 0。
- 环境实测正常：Ollama 进程在跑（ollama.exe PID 18780），`http://127.0.0.1:11434` HTTP 200；3 个模型（qwen3.8-q5-262k:latest / qwen3.8-q5-vision:latest / smtek/Qwen3.8-27B:Q5_K_M）；`/v1/models` 与 `/v1/chat/completions`（OpenAI 兼容）实测可用；`/api/show` 显示 capabilities 含 tools/thinking(/vision)；`[system-proxy] detectSystemProxy result: (none)` 无代理干扰；账号 personal/free，`EnableEnterpriseCustomModelPolicy` 仅对 enterpriseId 非空的企业账号生效。
- 从 app.asar 反查到的关键事实（版本 5.5.4，resources/app.asar）：
  - 自定义模型配置文件：`%USERPROFILE%\.workbuddy\models.json`，兼容「顶级数组」与「{models:[...],availableModels:[...]}」两种格式；支持 `${VAR}` 环境变量插值（apiKey/url）。
  - 条目合法字段（isValidLocalCustomModel）：id 必填非空；name/vendor/url/apiKey 字符串；maxInputTokens/maxOutputTokens/temperature 数字；supportsToolCall/supportsImages/supportsReasoning/onlyReasoning/useCustomProtocol 布尔；reasoning 对象。
  - `CustomModelIdPrefix=true`：应用给本地自定义模型 id 自动加 `custom-local:` 前缀（13 字符），发请求前会剥掉还原真实 model id。
  - `SkipToolCallSupportCheck=false`：若模型 `supportsToolCall` 不为 true，WorkBuddy 会把请求体里的 tools/tool_choice 删掉 → Agent 模式无法调工具。
  - Ollama 供应商预设：displayName「Ollama 本地」，providerType=local，authMode=none（不需要 API Key），endpoint=`http://localhost:11434/v1/chat/completions`，defaultSupportsToolCall=true，defaultSupportsImages=false，defaultSupportsReasoning=false，modelInputMode=input（手填模型名）。
- 待办：用户未确认走哪条通道；已提出可直接代写 `models.json`（应用会热加载）或引导走 UI。

## llm-wiki 二次消化：6 份手册 → 首份 synthesis 页
- 用户要求「消化」raw/guides/ 下 6 份 *-guide.html。首轮 ingest 已于 09-09 完成（6 source + 9 concept + 3 entity），本次为跨源二次消化。
- 全文重读 6 个 source 页；health.py 复检全绿。产出 `wiki/syntheses/six-guides-digest.md`（wiki 首份 synthesis）：五问五答知识地图（桌子多小→U型→少占→外置存取→循环干活→LLMWiki 元层）+ 三条贯穿原则（稀缺分治/检索第一性/可审计压倒黑盒）+ 行动映射（minute-lesson/2023exc/WorkBuddy 记忆分层/skill 审计）+ 遗留候选（渐进式披露建议独立成页；Top 5 价值候选待勾选）。
- 同步更新 index.md（Syntheses 区）、overview.md（延伸综合链接 + last_updated）、log.md（追加 query 条目）。health 复检 20 页全绿，新页 10 条双链全部命中。

