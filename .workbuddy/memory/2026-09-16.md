# 2026-09-16

## LLM Wiki 每日维护（自动化 a6e7b4ca）
- 执行 health + lint 结构体检（跳过 LLM 评估），20 页全绿、0 断链
- 修复 4 个稀疏页面的出链密度（LostInTheMiddle / ReAct / AndrejKarpathy / WorkBuddy）
- 于 wiki/log.md 追加 `[2026-09-16] daily maintenance` 条目
- 产出当日 Top 5 价值概念：渐进式披露、检索式练习、子代理与上下文隔离、费曼学习法、自动记忆
- 发现：llm-wiki-agent 仓库 raw/guides 与 wiki/ 各子目录仍未纳入 git，与「Git 管控一切」原则不符，已上报待确认

## 同日晚些（宏哥指令：提交 + 全部创建 + 定时任务改全自动）
- 新建 5 个 concept 页：ProgressiveDisclosure / Subagent / AutoMemory / RetrievalPractice / FeynmanTechnique，全部符合 frontmatter 与出链 ≥2 规范，已登记 index.md 并加反向链接
- Wiki 规模 20 → 25 页；health.py 与 lint 结构检查全绿（0 断链 / 0 稀疏 / 0 缺失实体）
- 首次 git commit：ee3385b（33 文件 / +3812 行），工作区已干净
- 关键事实：llm-wiki-agent 的 origin 是上游 SamurAIGPT/llm-wiki-agent，无推送权限 → 只 commit 不 push；若需远端备份应先 fork
- 定时任务 a6e7b4ca 已改为完全自动化：零询问、每日最多自动建 2 页、raw 新文件自动 ingest、结束前自动 commit、不 push

## 知识图谱首次构建
- 依赖：build_graph.py 需 networkx（已装进托管 venv `C:\Users\yangjh\.workbuddy\binaries\python\envs\default`，版本 3.6.1）；语义推理 Pass 2 需 litellm + API key，未启用，用 `--no-infer` 跳过
- 结果：25 节点 / 87 边 / 3.48 边每节点 / 0 孤儿 / 5 社区 / 0 幻影枢纽 / 0 脆弱桥；god nodes 为 ContextWindow(17)、AgentMemory(15)
- 产物：graph/graph.json、graph/graph.html（vis.js 自包含，双击即开）、graph/graph-report.md
- 观察：新建的三张教学法页自成社区，整组与主线仅靠 ProgressiveDisclosure 单点桥接，后续可加固
- commit ed924e5

## Python 基础语法四次课教学方案（面向统计分析）
- 需求：4 次课 × 2 × 45 分钟（共 6 小时）学完 Python 基础语法，后续用于统计分析
- 产出：
  - `notes/python-basics-course-plan.md`：课程目标（7 项可验证）、教学取舍、课时地图、四次课详案（含 45 分钟时间轴）、统计概念↔语法对照表、环境与数据集、验收评估与 6 题自测、复习与后续路线、备课提示
  - `datasets/scores.csv`：30 条学生成绩（id/name/gender/class/midterm/final/attendance），四次课复用同一份数据
- 关键设计决策：
  - 按「统计分析最小可用集」取舍——不讲面向对象、装饰器、正则、并发等对描述统计零边际收益的内容
  - 第 1 次课故意用 10 个变量算平均分（笨办法），作为第 2 次课引入列表的动机；第 2 次课用「列表套字典 = 一张表」为第 4 次课 pandas 埋伏笔
  - 明确点出 numpy.std（ddof=0，总体）与 statistics.stdev（ddof=1，样本）的差异，避免后续 t 检验翻车
- 结构对齐：新建 `notes/` 与 `datasets/`，与 README 声明的目录结构一致
- 复用 learn-studio 的 course 模板（课程目标 → 课时目录 → 时间分配 → 每课大纲 → 练习 → 预习复盘 → 验收标准）

## 方案改版：基础语法 2 次 + pandas 2 次（用户要求）
- 用户反馈：pandas 需要学 2 次，基础语法 2 次就够，每次课 90 分钟
- 重构产出 `notes/python-pandas-course-plan.md`（22KB），删除旧文件 `notes/python-basics-course-plan.md`
- 新结构：次1 语法 I（环境+类型+容器）、次2 语法 II（流程+函数+文件与 CSV 形态）、次3 pandas I（读入/勘察/选取/加列）、次4 pandas II（清洗/分组聚合/出图/导出）
- 核心设计决策：
  - pandas 是主战场，语法按「这条语法在 pandas 里还用不用得上」取舍（元组/集合/while/推导式降为"一句带过"）
  - 次 2 结尾用 `open` 逐行读（12 行）vs `pd.read_csv`（1 行）做动机对比，是全课程说服力最强的 30 秒
  - 硬性纪律：语法段超时只砍选讲内容，绝不占用 pandas 段时间；每次 90 分钟动手 ≥ 50 分钟
- 新增数据集：
  - `datasets/scores_messy.csv`（32 行）：刻意埋 6 类脏数据——重复行 id 3/17、`final` 与 `attendance` 缺失、id 13 期中为「84分」文本、性别 `男/男生/M/女 `（带尾空格）、班级 `一班/1班`、出勤 `0.96` 与 `95%` 单位混用、id 29 期末 118 异常值
  - `datasets/survey.csv`（32 条问卷）：定类（性别/专业）+ 定序（满意度 5 级、使用频率 4 级）变量，供 `value_counts`/`crosstab` 练习
- 已用本机 Python 3.13.12 校验：三份 CSV 均 7 列、无 ragged 行，脏数据类型与设计完全一致；三份文件都带 UTF-8 BOM → 第 3 次课第一次读数据必然撞上，正好讲 `encoding="utf-8-sig"`
- README 已补「课件与数据集」章节，与 notes/datasets 目录结构对齐

## guide 模式产出：python-basics-guide.html（第 1 次课上半场）
- 用 learn-studio 的 guide 模式（references/concept-guide.md）为「环境、Notebook、报错怎么读、变量与基本类型」生成溯源版单页手册
- 结构：header（3 chips）→ 01 它是什么（文档 vs 内核对照图）→ 02 核心设计（名字指向对象，含 b = a 的连线合并图）→ 03 运行全程（四步 + Traceback 解剖图）→ 04 五条铁律 → 05 十个一手出处卡片 → 06 六题三级自测（含计分）
- 溯源（全部一手，已验证可点击）：docs.python.org 教程 / errors（「最后一行说明错误类型」原文）/ executionmodel（「名称用于指代对象」）/ lexical_analysis / stdtypes / functions#type、PEP 8 命名、docs.jupyter.org What is Jupyter（REPL 与内核）、VS Code Jupyter Notebooks、python.org/downloads
- 实证修正：当前稳定版是 **Python 3.14.7（2026-08-05）**，3.15 计划 2026-10-01 发布 → 已把课程方案里的「Python 3.13」全部改为 3.14.7
- 质检（按 concept-guide 清单）：node --check 通过；quiz 答案分布 1,0,2,3,0,2；CSS 已用容器选择器收窄（.opts .opt / .rules .rule）；JS 字符串内无 ASCII 直引号
- 视觉自验（agent-browser）：整页 + 四段分块截图逐一看过，发现并修复两处——① 第 02 节底部连线穿过 b 方框（改为 a/b 堆叠 + 汇流母线）② 第 01 节 caption 折行留下单字孤行（缩短文案）
- 备忘：agent-browser 的元素级截图（`screenshot <selector>`）在本机不可用（SIGTERM），可靠路径是 `scrollintoview` + 视口截图；`set viewport` 之后页面会变空白，需重新 open
- 课程方案已补链接：第 1 次课条目指向该手册

## 新需求：系列化学习材料生成 skill（方案设计，待用户拍板）
- 用户指出「环境 / Notebook / 报错 / 变量与基本类型」是**四个独立概念**，应各出一页 guide；并要在 guide 模式之上建一个 skill，把「一组相关概念」变成成系列的学习材料
- 产出 `notes/concept-series-skill-design.md`：概念切分判据（一句话可定义 / 有一手来源 / 有独立自测点 / 有独立核心图）、四概念切分表与顺序、三个方案对比（A 薄编排器 / B 自包含工厂 / C 数据驱动生成器）+ 基线 D（不上 skill）、skill 契约、七步工作流（含"切分表未确认不得生成页面"卡点）、跨页一致性六条规则、上下文经济、反模式、验收标准
- 关键判断：
  - 现有 `python-basics-guide.html` 是"四个概念的平均分"，每节内容被压到只能给一句 lead + 一张图 → ② Notebook 讲不深（缺 nbformat、cell 状态）、① 环境几乎没讲（PATH/版本/包）
  - 推荐 A（薄编排器）：系列层只补三样——切分判据、跨页一致性、系列导航；单页细则仍以 learn-studio 的 concept-guide.md 为唯一事实源，避免双份维护漂移
  - 上下文经济是能否跑通的关键：一页约 400 行 HTML，必须"切分表先行 + 每页独立成轮 + hub 先做"
- 待确认三项：方案选型、粒度（4 页 / 6 页 / 先试水 1 页）、落地位置（repo 根 / guides/ 子目录）

## 落地：guide-series 技能 + Python 起步系列骨架
- 用户拍板：**方案 A 薄编排器 + 4 页 + 1 hub + `guides/` 子目录**
- 新技能 `~/.workbuddy/skills/guide-series/`（用户级，`agent_created: true`）：
  - `SKILL.md`：系列层职责（切分判据 / 来源池 / 调度 / 交叉链接 / 系列质检 / hub），单页规则全部指向 learn-studio 的 concept-guide.md（**不复制第二份规则**）；七步工作流，第 3 步「切分表未确认不得生成页面」是强制卡点；跨页一致性六条；上下文经济硬规则；反模式；验收标准
  - `references/series-contract.md`：目录与命名、concepts.md/sources.md 格式、hub 的 `PAGES` 数组约定（新页落地只改 `ready`）、导航片段（crumbs/pn HTML+CSS）、系列质检清单与命令、视觉自验命令
  - `assets/guide-skeleton.html`：单页骨架（token + 面包屑 + 六节占位 + quiz 引擎 + 上下页导航），复制即用
  - 记录本机两个 agent-browser 坑：元素级截图不可用（SIGTERM）→ 用 `scrollintoview` + 视口截图；`set viewport` 后页面变空白需重新 `open`
- 系列骨架 `guides/python-getting-started/`：`_series/concepts.md`（切分表 + 判据自检 + 依赖 + **先定 24 个自测考点防撞题** + 来源候选）、`_series/sources.md`（15 条一手来源去重池）、`index.html`（hub）
- hub 页含：概念地图（PAGES 数组驱动，`ready` 控制可点性）、顺序依赖 SVG（④ 回指 ①② 的虚线回路）、术语表（9 个术语统一措辞）、使用建议、进度勾选（localStorage + try/catch 内存兜底，保证 file:// 下地图必渲染）
- 自验：node --check 通过；截图看过概念地图、顺序图、术语表；用 `eval` 脚本点验勾选逻辑（checked=1 且出现「已学完」标签）；修掉一个真 bug（onchange 调用了未定义的 render）
- 待办：4 张单概念页（环境 → Notebook → 报错 → 变量），一页一轮

## guide-series 第二个系列：运算符与容器（第 1 次课下半场）
- 需求：为第 1 次课下半场出学习资料（运算符与 f-string、列表、字典、列表套字典 = 一张表）
- **动手前先查证据，改变了交付形态**：仓库在前几分钟被并行会话改过——README 新增 `guides/<系列名>/` 约定并写明「混合版单页正被上述系列逐页取代」；`~/.workbuddy/skills/guide-series/`（方案 A 薄编排器 + 4 页 + hub + `guides/` 子目录）已由用户拍板。因此**没有**沿用 guide 模式的根目录单页写法，直接按同一契约做了第二个系列
- 产出 7 个文件（148 KB）：`guides/python-operators-containers/` = hub `index.html` + 4 张单概念页 + `_series/{concepts.md, sources.md, checks.md}`
  - ① `python-operators-guide.html`（结果由类型决定：+ × ÷ 在 int/str 上的三行对照）
  - ② `python-list-guide.html`（切片是含头不含尾的尺）
  - ③ `python-dict-guide.html`（读一个键的三种结果：取到 / KeyError / get 默认值）
  - ④ `python-table-guide.html`（`rows["final"]` 一定报错 → 取一列得走过去）
- 每页六节（header → 01 它是什么 → 02 核心设计 → 03 运行全程 → 04 铁律 → 05 溯源 → 06 自测），每节一句 lead + 一张手写 SVG，4 页共 11 张图
- 事实全部本机 Python 3.13.14 实测，逐条列进 `_series/checks.md`（`-7//2=-4`、`-3**2=-9`、`round(2.5)=2`、`s[1:3]=[85,62]`、`list indices … not str`、`{unhashable}`、scores.csv 期末 77.33 / 期中 75.43 / 及格率 93.3%）
- 自测 24 题：三级各 2 题，答案索引打散（1,2,3,1,2,1 / 1,3,2,3,1,2 / 2,1,2,1,3,3 / 2,3,2,1,3,2）；先按 concepts.md 的「每页自测点」定考点，避免跨页撞题
- 质检：`node --check` ×5 通过；无裸 `.opt`；内部链接 19 条 0 断链；核心图逐页无头截屏核验
- **踩坑与修复**：
  - 真 bug：4 页的「系列首页」写成 `../index.html`，而 hub 在同一目录 → 全改 `index.html`（链接扫描发现，肉眼看不出来）
  - 契约对齐：初版自造 `.nav` 卡片式导航 → 按 `series-contract.md` 换成 `.crumbs` + `.pn`（首尾页用 `<span></span>` 占位）
  - 4 处 JS 字符串里混入 ASCII 直引号（中文引用 + `rows["final"]` 这类代码样例）导致整个 quiz 不渲染 → 含直引号的条目改用单引号 JS 字符串，中文引用统一「」
  - SVG 里等宽文本宽度估算失误：`sum(...) / len(...)` 在 151px 格子里顶边框 → 缩为 `sum / len`
- 新发现已回写契约 `guide-series/references/series-contract.md`：agent-browser 守护进程静默 SIGTERM 的后备路径 = 系统 Edge `--headless=new` + `:target` 单节视图（附 `.wrap > section{display:none}` 一屏查两处导航的技巧）；相对路径要按页面自身目录算，发布前必须跑链接扫描
- 遗留：根目录 `python-containers-guide.html` 是本次先写的单页混合版，随后被系列取代；已列入 README「历史版本」行，可删可留
- 协作提醒：本轮全程有并行会话在同一仓库写文件（`guides/`、`python/`、README、notes 都在动）；下次进这个仓库先 `ls -la` 看时间戳 + 读 README，再决定产出形态
